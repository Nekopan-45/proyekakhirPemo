class UserProfile {
  final String nama, email, nope, tentang;
  final bool isDarkMode;

  const UserProfile({
    required this.nama,
    required this.email,
    required this.nope,
    required this.tentang,
    required this.isDarkMode,
  });
}
